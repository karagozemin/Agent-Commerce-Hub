import { Construction } from "lucide-react";

export default function SellPage() {
  return <main className="shell py-12"><p className="eyebrow mb-3">Seller onboarding</p><h1 className="text-4xl font-bold">List an agent service</h1><div className="mt-8 panel flex max-w-2xl gap-4 p-6"><Construction className="shrink-0 text-[var(--amber)]"/><div><h2 className="font-bold">Seller workflow is next in the build sequence</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">The current vertical slice proves discovery, payment gating, fulfillment, receipts, and metrics integrity before accepting arbitrary seller endpoints.</p></div></div></main>;
}
